export interface ApiResponse{
    success: boolean,
    message: string,
    isAcceptingMessage?: boolean
}

// remeber u can add optional model or anything by following ?: syntax sir showed he used message model if in case
// message is needed to be send what format should that be in.. 